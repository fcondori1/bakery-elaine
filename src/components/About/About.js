import React from 'react';
import PropTypes from 'prop-types';

About.propTypes = {
    
};

function About(props) {
    return (
        <div>
            Hello from About
        </div>
    );
}

export default About;